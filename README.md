# Ultra Home Site

This is a Next.js 14 (App Router) project built with TypeScript, Tailwind CSS, Prisma and NextAuth.js. It provides a modern architecture portfolio site with authentication and user dashboards.

## Setup

1. **Install dependencies**

   Use a recent version of Node (>= 18). Install dependencies using npm:

   ```bash
   npm install
